# 2026-09-08 工作日志

## 00:05 主任务（澳门站开赛日）

- **窗口滚动**：09-06/07/08/09 → **09-07(昨) + 09-08(今) + 09-09(明) + 09-10(后)**。空窗期正式终结，9/8 起连续六天央视有直播（至 9/13）。
- **9/10 首次入窗并写入 4 场 matches**：11:35 陈幸同 vs 尼娜·米特兰姆、12:10 周启豪 vs 邱党（国乒）；12:45 佩索茨卡 vs 早田希娜、13:20 西蒙·高茨 vs 户上隼辅（日韩）。四场均在 CCTV-5+ 9/10 上午 11:00–14:30 窗口内。
- **9/10 epg 一手核对**：CCTV-5+ 11:00「第一轮5」→ 14:30 亚洲男排 1/4 决赛；CCTV-5 18:30「1/8决赛 1」→ 20:30 女篮世界杯 1/4 决赛（窗口 18:30–20:30）。
- **条件场不预写**：9/10 晚 18:30 已是 1/8 决赛、对阵待定 → 不写 matches，只在 note 留痕，待 9/9 滚动后补。
- **取舍留痕**：9/10 13:55 迪亚兹 vs 桥本帆乃香 虽在赛程内，但央视5+ 14:30 切走只能看开头 → 不写入 matches。
- 9/8、9/9 epg 与赛程第五次复核无变化，仅日期标签滚动，risk 全维持「✅低风险」。
- **video.json 不新增**：9/8–9/10 均有央视直播，视频兜底块本就不显示；9/7 为空窗昨日不渲染。已按八·乙在 note 留痕。
- 译名留痕：佩索茨卡 = 皮索特斯卡（乌克兰）。

## 踩坑

1. **`render.test.js` 末例「空窗说明应渲染成 pending__points」失败**。根因：窗口滚动后已无可见空窗天，`pending__points` 天然为 0。
   解法：在该例加「窗口内存在可见空窗天才断言」的守卫（自动休眠/恢复）。**未动 app.js**。
2. **第一版守卫仍失败** —— 用了 `new Date().toISOString().slice(0,10)`，UTC 比北京晚 8 小时，凌晨 00:0x 取到「昨天」，把 9/7 误判为可见空窗天。改成 `getFullYear/getMonth/getDate` 本地拼接后 89/89 通过。
3. `nextEvent.note` 追加内容第一版未命中分类关键词，落进 `meta` 使首段变长 → 改写为带「直播/赛程/国乒」关键词的句子后全部落进 tv/sched，meta 恢复原长。

## 验证

- `node --test tests/*.test.js` → **89 pass / 0 fail**。
- 解析层：9/7 dayNote 要点 12 条、最长 53 字、括号配对、next 抽到、squad 7 / absent 7。
- 渲染层：`<section>`=4、`emeta__rank-item`=12、`next__lines`=2、关键词全命中。
- 推送：data.json commit `363c3c2`，早报 commit `740510a`；远端 curl 复核 `updatedAt = 2026-09-08 00:05`、days = 09-07/08/09/10。

## 单链

- 9.2 实算：C={9/8 10:00, 11:45, 17:30, 18:05}（4 簇），T_next = **9/8 10:00**，与现存 once「刷新链-20260908-1000」完全一致 → 保持不动，不删重建。

## 下轮待办

1. 9/8 首轮赛后回填 `result`（11:00 陈熠、12:45 陈垣宇优先）。
2. 9/9 滚动为今日后，补写 9/10 晚 18:30 的 1/8 决赛对阵。
3. 关注 9/10 上午 13:55 桥本帆乃香场次是否仍在窗口内。

---

## 08:25 主任务（开赛日上午复核）

- **窗口不滚动**（今日仍 9/8）→ 四日期维持 09-07(昨) + 09-08(今) + 09-09(明) + 09-10(后)。9/7 昨日块原样保留一字未改。
- **三轮复核全部对上，matches 零改动**：9/8 epg 开赛前再核 —— 央视5+ 11:00「第一轮1」→ 14:37 女篮录像；央视5 18:00《体育新闻》、18:30「第1轮 1」→ 21:30《体育世界》。9/9（5+ 11:00–14:36 / 5 18:30–21:30）、9/10（5+ 11:00「第一轮5」/ 5 18:30「1/8决赛 1」→ 20:30 女篮）同样无变化。四节均**无插播**，risk 全维持「✅低风险」。
- **9/8 全天 13 场 1/16 决赛**（7 男单 + 6 女单）由 4 个新独立源交叉确认（腾讯 20260908A00XZZ00、搜狐 1073028663 / 1072857873、头条 7682923940896916010），国乒当日仅陈熠 11:00、陈垣宇 12:45 两场，无临时改期、无新增退赛。已把这些新源补进各场 `sources`。
- **黄友政 9/9 = 11:35 再次确认**：优个网「9/8–9/9 完整赛程」与头条 1875633103287371 均写 11:35；腾讯 20260907A007TR00 的 11:45 仍是少数派 → 维持 5 源对 2 定案，不翻案。
- **收录口径定型（新）**：本站每日固定收录 **4 场 = 国乒场次 + 日本焦点**。窗口内其余场次（9/8 另有 11:35/12:10/13:20/13:55/14:30、19:40/20:15/20:50）不写进 matches，改在**当日早报正文**列全，避免信息盲区又不撑爆卡片。
- **窗口判据再验证**：9/8 15:05 卡尔伯格、9/9 15:05 塔卡电视看不到（上午窗 14:37/14:36 就切）；**9/8 14:30 大藤沙月离切播只剩 7 分钟**，同样不写 —— 这条是「只剩几分钟」型的新实例。
- **video.json 不新增**：9/8–9/10 均有央视直播，兜底块不显示；已按八·乙在 note 留痕。译名留痕：皮索特斯卡 = 佩索茨卡（与上轮一致）。

## 验证（08:25 轮）

- `node --test tests/*.test.js` → **89 pass / 0 fail**。
- 解析层：9/7 dayNote 要点 12 条、最长 53 字、括号配对、next 抽到、`parseNextRoster` → squad 7 / absent 7。
- 渲染层：`<section>`=4、`emeta__rank-item`=12、`next__lines`=2、`pending__points`=0（无可见空窗天，符合预期）、12 个选手关键词全命中。
- 推送：data.json commit `2b8332c`，早报 commit `15cbab8`；远端复核 `updatedAt = 2026-09-08 08:25`。

## 单链（08:25 轮）

- 9.2 实算：C = {9/8 10:00, 11:45, 17:30, 18:05} ∪ {9/9 10:00, 10:35, 18:05, 18:40} → 8 簇（相邻均 >30min，无一合并）；T_next = **9/8 10:00**，与现存 once「刷新链-20260908-1000」完全一致 → 保持不动。

---

## 09:50 本机轮（赛事已开打徽章铁律 · 上线）

子上 09:49 截屏：「下一站 WTT 澳门冠军赛 9/8-13」卡片右侧挂着「距今天 0 天」徽章。反馈原话：「赛事已经开始就不用显示这个了. 记住」。

**根因**：buildBelow (app.js:2063) tag--soon 徽章只看 gapMode，缺赛事已开打守卫。同时 nextEventCompact / nextEventCompactNoDate (app.js:1380/1400) 也无此守卫（简版倒数日同根）。

**数据惊讶**：data.json 中 `nextEvent.date = 2026-09-08`，但 `daysAway = 7` —— 采集端错填（按语义应该是 0）。截图的「距今天 0 天」是哪来的？可能是另一台机器 / 旧缓存。

**修法**：以**真实日期**为准（不依赖采集端 daysAway 字段）：
- 新增 `isNextEventStarted(ne, today)` 工具函数（`ne.date ≤ today` → true）
- 三个使用点都加守卫：`nextEventCompact` / `nextEventCompactNoDate`（参数加 now）/ `buildBelow` 的 tag--soon 徽章
- 即便 daysAway 误填 7+date=今天 也隐藏徽章（采集端错填容错）

**App.js 改 5 处**：
- line 1378–1390 新增 isNextEventStarted 工具函数（含注释）
- line 1392 nextEventCompact daysAway>0 + !isNextEventStarted
- line 1412 nextEventCompactNoDate 同上
- line 2063 buildBelow 徽章：ctx.gapMode || isNextEventStarted(ne, ctx.now)
- line 2158 导出清单加 isNextEventStarted / nextEventCompact / nextEventCompactNoDate

**测试追加 9 条**（logic.test.js 末尾）：isNextEventStarted 真值表 5 条 + nextEventCompact 渲染断言 3 条 + nextEventCompactNoDate 同根断言 1 条。全过 **98/98**。

**验证冒烟**（真实 data 跑 buildBelow）：
- `isNextEventStarted(9/8) = true`，`gapMode = false`
- `buildBelow` 不含「距今天 7 天」、不含任意「距今天」、`buildAbove` 也不含 ✅

**Git 流**：
- 分支：`fix/next-event-countdown-zero-days`（基于最新 origin/main）
- commit：`9d182bd fix: 赛事已开打不再显示「距今天 X 天」(徽章+简版同步)`
- 推 fix → 切 main → fast-forward merge → push main → Actions 部署 `01:59:56Z` completed ✅
- `rev-list --left-right --count HEAD...origin/main` = `0 0` 确认同步
- curl 线上 app.js 含 `isNextEventStarted` 5 处命中

**后续动作**：MEMORY.md 加一条倒数日铁律；考虑是否要让采集端补 daysAway 自校验（暂不动采集端）。


---

## 11:32 本机轮（下一站模块窗口隐藏 · 上线）

子上 11:32 二次反馈：9/8 澳门站已开打，「下一站国乒赛事」模块可以不要了。原话：「那个模块出现的条件是『今天明天后天已经没有当前赛事的安排了』」。

**根因**：`buildBelow` (app.js:2071) 模块渲染条件只看 `data.nextEvent && data.nextEvent.date`，没判定当前赛事是否已落入今/明/后三天窗口。

**数据确认**：`nextEvent.date = 2026-09-08`（今天，采集端 08:25 已把 daysAway 从 7 修回 0）；days 四日期 9/7(昨空窗)/9/8/9/9/9/10 全有 WTT 澳门冠军赛比赛 → 今/明/后三天就是当前赛事，模块应消失。

**修法**：新增 `isNextEventWithinWindow(ne, today, horizonDays=2)`（`ne.date <= addDays(todayKey, 2)` 即 `[今天, 今天+2]` 窗口），`buildBelow` 模块渲染条件加 `!isNextEventWithinWindow(data.nextEvent, ctx.now)` 守卫。判据用真实日期（不依赖 daysAway）。上一轮「倒数日徽章」铁律（isNextEventStarted）范围更窄（仅 today），本铁律 ±2 天覆盖整模块隐藏，模块没了徽章自然也没。

**App.js 改 3 处**：
- line 1388 新增 isNextEventWithinWindow（含注释，复用 addDays）
- line 2071 buildBelow 模块渲染条件加守卫
- line 2181 导出清单补 isNextEventWithinWindow

**测试追加 9 条**（logic.test.js）：窗口真值表 7 条（今/明/后/大后天/一周/缺参/自定义horizon）+ buildBelow 真实数据模块消失 + 窗口外仍渲染 各 1 条。全过 **107/107**。

**验证冒烟**（真实 data 跑 buildBelow）：`isNextEventWithinWindow(9/8) = true`，`buildBelow` 不含「下一站国乒赛事」、不含 `day--next`，`buildAbove` 同样不含 ✅

**Git 流**：
- 分支：`fix/next-event-module-hidden-in-window`（基于最新 origin/main 7ad189c）
- commit：`91c27ea fix: 今天/明天/后天有当前赛事安排时,「下一站国乒赛事」模块整体隐藏`
- 推 fix → 切 main → ff merge → push main → Actions `03:39:45Z` completed ✅
- `rev-list --left-right --count HEAD...origin/main` = `0 0`
- curl 线上 app.js 含 `isNextEventWithinWindow` 3 处 + buildBelow 守卫确认

**后续**：MEMORY.md 加「下一站模块窗口铁律」条目（与倒数日铁律相邻）。
