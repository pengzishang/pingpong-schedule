# 项目记忆: pingpong-schedule 央视乒乓赛程

> 完整指令在项目根 `采集说明.md`（唯一指令源）。本文件只记**踩坑与判据**，不重复指令书。

## 铁律速查
1. **updatedAt 必刷**：每次跑完无论有无变更都 bump + push。诚实性靠 note 如实写「本轮复核未发现数据变更」，**绝不伪造赛果**。
2. **写完即推送**：data.json 写完 + `json.load` 通过 → 立刻 commit/push，早于早报/记忆/单链对齐（8/23 事故）。
3. **JSON 硬校验**：改完必跑 `json.load`，**只依赖是否抛错**，不要断言字符串里没有 `\n`。复杂转义别塞 `python -c`+单引号，写临时 .py 再删。
4. **先拉后采/先拉后推**：读取或 push 前 `git fetch origin && git rebase origin/main`。禁止强推。

## 单链滚动刷新（once 链）
- 全局同时仅 1 个 name 含「刷新」的 once；建新前先删旧。命名 `刷新链-[YYYYMMDD]-[HHMM]`，`modelId` 必须 `hy3`，`cwds` 用 `C:/Users/pengz/Documents/GitHub/pingpong-schedule`。
- 刷新时刻：赛前 T=K-1h（仅 T∈(now,now+30h]）；赛中整链只加 1 个 now+30min 点；升序后 30 分钟聚类（≤30min 并入取最晚）；取 ≥now+30min 最早簇为 T_next；无候选则链暂歇。
- **凌晨场日期修正**：data.json 按「08:00~次日08:00」归类，00:00-07:59 的场真实开赛是**次日**；K 须修正，否则链假暂歇。
- 赛中区间 = 当前 ∈ [K-30min, K+3h]。`live` 字段：已开赛且 result 为空时写进行中局分；result 一出即清 live。

## git / 推送
- 只推 origin（github.com/pengzishang/pingpong-schedule）；gitee 已弃用。
- 提交信息：`赛程刷新_YYYY-MM-DD_HH:MM` / `每日赛程更新 YYYY-MM-DD` / `早报存档与记忆_YYYY-MM-DD_HH:MM`。
- push 被拒（non-fast-forward）：`stash` 无关 → `fetch` → `reset --hard origin/main` → `checkout <已校验 commit> -- data.json` → commit → push。**勿强推**。
- **rebase 被 unstaged changes 挡住**：先提交改过的 `.workbuddy/memory/MEMORY.md` 再 rebase（9/3 已遇）。

## 数据约定
- days 四日期：昨日（带 result 战报，原样保留）+ 今日 + 明日 + 后日。subtitle 固定。
- risk 仅未结束场填：🔴/⚠️/✅ 开头 + 括号大白话标签 + 口语原因建议（台号写 `央视5`/`央视5+`）；整段 2~3 句。已结束省略。
- 「下一站…」句内**禁句号**，句内用逗号分隔出战/缺席名单。
- **nextEvent.note 名单句必须写成 `国乒N人:男单…/…,女单…/…,缺席:…。背景:…`**。`parseNextRoster` 正则只认 `出战|国乒\d*人|报名|参赛|阵容`+冒号（「国乒男单5人:」不匹配 → 0 人）；「缺席:」lookahead 会吞到下一关键词 → 用 `背景:` 截断。日期块里「下一站…出战:」写法不受影响。
- 【9/3 判据】`nextEvent.note` 追加文本禁用 `→`：`parseNextEventNote` 会把它归进 schedule 段，触发 `renderScheduleOrTvHtml` 时间线分支 → render.test.js「赛程/直播描述段应拆成多行列表」失败。**症状指向 nextEvent 卡，不在 dayNote**。处置：只改采集端文案，不动 app.js。
- **【契约 4.6.2】空窗天整段说明写入 day 顶层字符串 `dayNote`，`schedule` 置空 `[]`**。
- **`dayNote` 首句铁律**：`cut = first.search(/[（(：:]/)`，**只有 `cut > 3`** 才取 `substring(0,cut)` 作 lead，否则退回 44 字截断。首句须是「空窗期延续(…复核)。」这类短结论；反例 `9/1(周二)……`（左括号索引 3）必挂。其余要点每条 ≤100 字、用「。」分隔、括号成对；末尾保留「下一站…」句。
- **验证 dayNote 必须两层都做**：① 解析层 `api.parseDayNote` 查 lead<20、points≥8、最长≤100、括号配对、next 抽到；② **渲染层 `buildAbove+buildBelow` 数 `<section>` 与 `pending__points`**。单看 ① 正常不代表页面正常。
- 修前端解析问题优先**在采集端改写法**，不动 app.js。**验证手法**：`node -e "const {loadApp,loadJSON}=require('./tests/_harness.js'); ..."` —— app.js 末尾有 `module.exports`，Node 里可直接 require。
- **例外判据**：若故障字段恰是新契约禁用的 `schedule`、采集端无合规字段可绕过 → 允许最小修补并**显式通知前端机**（9/1 已在 app.js:683 补 `dayNote` 判空）。
- video.json 顶层仅 `days[]`，用 `platform`（非 channel）。`renderVideoBlock` **不渲染 result** → 赛果写进 `note` 首句。团体赛按 5.3.1：playerHome/Away 填队名，关键球员进 Info，nationHome 填中国以进「国乒」标签页。
- 前端测试：`node --test tests/*.test.js`（**不能** `node --test tests/`）。**每轮改数据后必跑，0 fail 才推送。**

## 采集环境（踩过的坑）
- **epg 参数被丢**：改用 `sports.cctv.com/epg/index.shtml?channel=cctvX&date=YYYY-MM-DD` 稳定命中。可能返回旧日期缓存，须核对日期导航栏高亮。
- `WebFetch tv.cctv.com/live/cctv5plus/` **9/1 起失效**；CCTV-5 走 index.shtml + 搜狐/网易当日汇总稿交叉。
- **咪咕三入口**：① banner（「正播…」/「X时播…」）；② 资讯栏首条；③ 「精彩回放」区。**排期会变，每轮必重抓，禁止沿用上轮时段**。
- **咪咕入口会失效**：`s.miguvideo.com/ZZZVqn` 9/2 起 404；专区 404 时改走 `miguvideo.com` 搜索页（仍可拿官方预约列表），或 WebSearch 间接命中，**并在 note 留痕**。
- 失效源黑名单：sports.sina.com.cn/pingpang、sports.163.com/pingpang、s.weibo.com、so.toutiao.com、**sports.sina.cn 首页聚合稿（AI 体例）**、**k.sina.cn 聚合稿（场馆口径必错）**、**cnyouth.com（英文旧稿站）**。
- **场馆口径唯一权威**：澳门 = **澳门东亚运动会体育馆**；亚运乒乓 = **丰田天空大厅**。见到其他馆名一律判错。
- `epg.pw` 默认 America/Chicago，**+13h = 北京时间**。
- 单回合无法回收后台 subagent 结果 → 采集走「编排器自采」更可靠。

## 真伪判定
- **AI 幻觉赛果**：k.sina.com.cn 大量「本文由AI生成」稿把预测写为既成赛果。可行性校验：K + 局数×8min ≤ 当前，否则排除。
- **旧闻翻新** / **跨赛事旧战报**：拿赛事名+对阵反搜原始通稿核对发布日 / 核对「下一轮对手」是否与本站签表一致。
- **单一平台多稿矛盾 ≠ 事件为假**。分层：① 要素多平台一致 → 为真；② 仅直播渠道未官宣 → 只影响是否写文件；③ 个别夸张细节无出处 → 只剔该细节。处置：不写入但必须在 note + 早报留痕。
- **条件场作废**：上轮「若 A 胜则某日某时」本轮必查条件是否成立；A 出局则删条件句并补明确否定句，不能静默删。
- 权威源优先级：@WTT世界乒联 微博（uid 7556183438）逐局赛果 = 官方口径，单源可写 result；虎扑/直播吧/腾讯新闻正文次之。终局后 5~15 分钟发稿 → 开赛超 1 小时仍未发 = 仍在进行，别套旧比分。
- WTT 冠军赛男女单各 32 人正赛、**不设资格赛**；「需从资格赛打起」一律判错。

## 前端已知行为（不是 bug，别误修）
- **昨日空窗天不渲染「昨日战报」区**：`prepareCtx`(app.js:1632) 的 `pastContent` 只看 `day.matches` 有无央视场次（dayNote 判空只加在 `upcomingContent`）。**判定：合理设计**。不改 app.js、不通知前端机。
- 因此「`<section>` 数 = 4」= 今天/明天/后天三块 + 下一站卡；昨日不算，这是正常值。

## 直播渠道判定
- **主办方乒协稿只提当地台 ≠ 国内无版权**（连续三轮错判）。低级别站次**必查咪咕专区一手排期**。
- 有直播但对阵未公布 → 按 5.3 无法填 playerHome/Away，**不要硬凑**，改在当日 dayNote 写明时间+平台+观看方式（八·乙）。
- **国乒零报名的全外协低级别站即使有咪咕直播也不写 video.json**，但必须在 note 留痕理由。
- **国乒选手的海外联赛（如樊振东德甲）有国内平台转播 → 必须写 video.json**，按 5.3.1 团体赛写法。

## 赛事背景（2026 下半年，仅留判据与指针）

> 完整名单与赛历逐轮留档在 `data.json` 的 `nextEvent.note` 与 `每日乒乓球赛事早报/`。

- **WTT 澳门冠军赛 9/8-13（澳门东亚运动会体育馆）**：国乒 9 人（男单王楚钦卫冕·头号种子/林诗栋8号/周启豪/黄友政WTT提名/陈垣宇递补；女单蒯曼2号/陈幸同/王艺迪/陈熠）。莎昱轮休。1000 分、80 万美元；1/16 与 1/8 五局三胜、1/4 起七局四胜。女单头号种子张本美和，韩国全员弃赛。
  - 咪咕逐日排期：9/7 19:00 前瞻 → 9/8、9/9 各 11:00+18:30 1/16 → 9/10 11:00 收尾、18:30 转 1/8 → 9/11 两节 1/8 → 9/12 两节 1/4 → **9/13 11:00 半决赛、18:30 决赛** → 9/14 19:00 复盘。
  - **抽签至 9/3 仍未进行（连续 8 轮）**。四类不实信息：①自媒体种子分区推演；②cnyouth 英文旧稿；③sina.cn 系旧稿/AI 场馆错稿；④「国乒 10 人、含温瑞博/向鹏」稿（搜狐 1071171250）。**快速识别标记：场馆名错 / 名单出现温瑞博或向鹏 = 必是 8/28 前旧稿。** 必须在 nextEvent.note 显式披露。
  - **【9/3】抽签首次有预计时点：9/6 下午**（同步欢迎晚宴），王楚钦、林诗栋预计 9/5-6 动身。仅头条「莱山发布」一家、未获 WTT/澳门体育局确认 → **标注「仅作时间参考」**。
  - 外协·中国台北 4 席：林昀儒(第6)、郭冠宏(第38)、郑怡静、叶伊恬(第41)。
- **阿拉木图常规挑战赛 9/1-6（当地连续第五年承办）**：国乒零报名，正赛 9/3-9/6，9/6 14:00 四项决赛。**实际比赛 13:00 午场 / 19:00 晚场；咪咕预约挂 14:00/20:00 = 转播窗口开始时间，比首场晚约一小时**（9/3 定案）。
  - 9/2 赛果（≥5 源）：方博/哈吉 **3-0 马尔金/卡西姆(11-5、11-8、11-6)**；倪夏莲 **3-2 科迪罗娃(11-5、11-4、9-11、7-11、11-9)**。
  - 正赛签表：方博落 7/8 区，男单首轮 VS 德国**孟繁博**，次轮对 格拉斯门科/及川瑞基 胜者；早田希娜首轮 VS 李昱谆。**种子顺位多稿打架 → 只写对阵不写种子号。**
