# 国乒赛事日历 · 采集与刷新任务指令书

> 本文件是采集端自动化任务的**唯一指令源**。自动化任务运行时读取本文件执行，任务规则一律以本文件为准，修改本文件即修改任务，无需改动 prompt。
> 本文件**完整、自包含**，不依赖任何其他文档。

---

## 〇、角色判定（读本文件的 agent 先看这里）

自动化任务分**两个角色**，运行时先按自己 prompt 里声明的角色定位，执行对应章节：

| 角色 | 判断依据 | 执行章节 |
|------|----------|----------|
| **每日早报主任务** | recurring 定时任务（每 8h），prompt 声明「你是主任务」 | 第一 ~ 八章 + 第九章「9.3 主任务角色」 |
| **轻量刷新任务** | once 一次性任务，任务名含「刷新链」，prompt 声明「你是轻量刷新」 | 第九章「9.4 轻量刷新角色」 |

两个角色共同遵守的自动化工具调用约定见第十章。

---

## 一、采集任务总览

- **采集时间窗**：今日 08:00 至次日 08:00（及后日临近场次一并采集）。
- **转播范围**：央视 CCTV-5 / CCTV-5+ / CCTV-16 播放的所有乒乓球比赛（含国乒、日本、韩国及其他协会选手）。
- **产出物**：
  1. `data.json` —— 央视电视直播赛程
  2. `video.json` —— 咪咕 / 央视频等视频平台直播赛程
  3. `每日乒乓球赛事早报/YYYY-MM-DD.md` —— 当日早报存档（仅主任务产出）

---

## 二、采集来源与交叉认证

### 2.1 采集来源（按组分）

- 组①：央视官网 sports.cctv.com（若 403 如实标注）、电视猫 tvmao.com
- 组②：新浪体育、腾讯新闻 / 腾讯体育、今日头条
- 组③：网易体育、搜狐体育、直播吧
- 组④：WTT 官网 / 咪咕体育、央视总台节目单页

### 2.2 交叉认证规则

- 同一场比赛被多组报到 → 合并证据、不重复计数；时间取**多数源众数**，差异在备注列出。
- 国乒场次需 **≥4 个独立源**；日韩 / 外协场次可少于 4 源。
- 仅 2~3 源一致的场次，标「⚠️ 证据不足（仅 N 源）」照常展示。
- **同一内容被多家转载不算独立源**（如头条原文被网易转载只计 1 源），只有独立编辑 / 独立域名的才算独立源。

---

## 三、两个数据文件总览

| 文件 | 内容 | 顶层结构 |
|------|------|----------|
| `data.json` | 央视（CCTV-5 / CCTV-5+ / CCTV-16）转播的乒乓球比赛 | `title` `subtitle` `updatedAt` `note` `days[]` `nextEvent` |
| `video.json` | 咪咕 / 央视频等非央视平台直播的乒乓球比赛 | 仅 `days[]` |

两个文件放在同一目录（`pingpong-schedule/`），网页按 `date` 字段自动合并展示。

---

## 四、data.json 完整规范

### 4.1 顶层字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `title` | 字符串 | 固定填 `国乒赛事日历` |
| `subtitle` | 字符串 | 固定填 `央视直播 · 乒乓球赛程 · 每日更新(可筛选 全部/国乒/日韩)` |
| `updatedAt` | 字符串 | 本次生成的日期时间，格式 `YYYY-MM-DD HH:MM` |
| `note` | 字符串 | 可选，采集过程日志等任意文本 |
| `days` | 数组 | 日期数组，见 4.2 |
| `nextEvent` | 对象 | 见 4.5，仅当未来 3 天无比赛时填写 |

### 4.2 day 对象（`days[]` 每一项）

| 字段 | 类型 | 说明 |
|------|------|------|
| `date` | 字符串 | `YYYY-MM-DD` |
| `weekday` | 字符串 | 周几，如 `周六` |
| `matches` | 数组 | 当天央视转播的乒乓球场次，见 4.3；无比赛则为 `[]` |
| `schedule` | 数组 | 当天央视转播安排说明（未公布对阵时填），见 4.6；无则为 `[]` |
| `pending` | 布尔 | 当天对阵未公布时为 `true`，否则 `false` |

`days` 数组固定保留**四个日期**：昨日（带 `result` 战报）+ 今日 + 明日 + 后日。不要包含昨天之前更早的日期。
刷新时，**昨日战报块原样保留**，只刷新今日 / 明日 / 后三日。

### 4.3 match 对象（`days[].matches[]` 每一项，央视场次）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `time` | 字符串 | 是 | 开赛时间 `HH:MM` |
| `channel` | 字符串 | 是 | 央视台，如 `CCTV-5` / `CCTV-5+` / `CCTV-16` |
| `playerHome` | 字符串 | 是 | 主方选手名 |
| `playerHomeInfo` | 字符串 | 否 | 主方补充信息，如 `世界第1` |
| `playerAway` | 字符串 | 是 | 客方选手名 |
| `playerAwayInfo` | 字符串 | 否 | 客方补充信息 |
| `stage` | 字符串 | 否 | 轮次，如 `女单1/4决赛` |
| `risk` | 字符串 | 否 | 仅未结束比赛填，完整写法见 4.4 |
| `result` | 字符串 | 否 | 仅已结束比赛填，精简比分；未结束则省略此字段 |
| `sources` | 数组 | 否 | 确认本场的来源名称列表，如 `["央视官网","电视猫","新浪体育"]` |
| `tournament` | 字符串 | 否 | 赛事名，如 `WTT欧洲大满贯瑞典站` |
| `nationHome` | 字符串 | 否 | 主方国籍，如 `中国` / `日本` / `韩国` / `法国` / `中国台北` |
| `nationAway` | 字符串 | 否 | 客方国籍 |
| `replay` | 对象 | 否 | 央视重播，见 4.7 |

- `result` 示例：`向鹏 3-1 西多伦科(11-8/9-11/11-9/11-8)`。
- `nationHome` / `nationAway` 填选手国籍，网页会按国籍自动归入「全部 / 国乒 / 日韩」标签页。

### 4.4 risk 字段完整写法（仅未结束比赛填写）

- 以 `🔴 高风险` / `⚠️ 中风险` / `✅低风险` 开头。
- 括号内用**大白话类型标签**，如 `电视看不全`、`被插播打断`；禁止 `转播段尾部截断型` 这类内部行话。
- 冒号后用**简单直白的口语**写原因 + 建议（长辈能看懂；台号写 `央视5`、`央视5+`）。
- 已从节目单核实存在插播风险的场次，必须标注 `⚠️ 可能被 XX 节目插播，建议用央视频全程观看`。
- 有证据可靠性提示，用 `⚠️` 另起一句。
- 有人头对战数据，用 `📊` 另起一句。
- **整段控制在 2~3 句**，不堆砌。

### 4.5 nextEvent 对象（仅未来 3 天无比赛时填）

触发条件：未来 3 天（今日 / 明日 / 后日）的 `matches` 全部为空时，额外抓取「下一场国乒赛事」的日期写入。

| 字段 | 类型 | 说明 |
|------|------|------|
| `date` | 字符串 | `YYYY-MM-DD` |
| `weekday` | 字符串 | 周几 |
| `daysAway` | 数字 | 距今天数（整数） |
| `note` | 字符串 | 赛事说明，如 `WTT欧洲大满贯瑞典站 1/4决赛` |

`nextEvent` 与 `days` 数组在同一层级。

### 4.6 schedule 对象（`days[].schedule[]` 每一项）

| 字段 | 类型 | 说明 |
|------|------|------|
| `time` | 字符串 | 时段 `HH:MM` |
| `channel` | 字符串 | 央视台 |
| `tournament` | 字符串 | 赛事名 |
| `content` | 字符串 | 内容说明，如 `乒乓球女单1/4决赛` |

`schedule` 只放**乒乓球**直播 / 转播窗口。以下两类一律不写入：
- 录像 / 录播（标注「录像」「录播」「实况录像」「往届录像回顾」「非本站直播」的时段）
- 非乒乓球栏目（篮球含三对三 / 网球 / 田径 / 游泳 / 足球 / 排球 / 斯诺克 / 拳击 / 《体育新闻》等）

只保留 WTT / 世乒赛 / 世界杯 / 冠军赛 / 大满贯等乒乓球赛事窗口（内容含「乒乓 / WTT / 冠军赛 / 大满贯 / 世乒 / 单打 / 决赛」等关键词的才写入）。

### 4.7 replay 对象（match 的 `replay` 字段，可选）

| 字段 | 类型 | 说明 |
|------|------|------|
| `time` | 字符串 | 重播时间，如 `次日 10:00` |
| `channel` | 字符串 | 重播台，如 `CCTV-5` |

### 4.8 data.json 完整示例

```json
{
  "title": "国乒赛事日历",
  "subtitle": "央视直播 · 乒乓球赛程 · 每日更新(可筛选 全部/国乒/日韩)",
  "updatedAt": "2026-08-22 12:05",
  "note": "采集过程日志（可选）",
  "days": [
    {
      "date": "2026-08-21",
      "weekday": "周五",
      "matches": [
        {
          "time": "19:30",
          "channel": "CCTV-5",
          "playerHome": "孙颖莎",
          "playerHomeInfo": "世界第1",
          "playerAway": "张本美和",
          "playerAwayInfo": "世界第2",
          "stage": "女单1/4决赛",
          "risk": "✅低风险(正常转播):本场央视5全程直播,放心看。",
          "result": "孙颖莎 3-1 张本美和(11-8/9-11/11-9/11-8)",
          "sources": ["央视官网", "电视猫", "新浪体育"],
          "tournament": "WTT欧洲大满贯瑞典站",
          "nationHome": "中国",
          "nationAway": "日本",
          "replay": { "time": "次日 10:00", "channel": "CCTV-5" }
        }
      ],
      "schedule": [],
      "pending": false
    },
    {
      "date": "2026-08-22",
      "weekday": "周六",
      "matches": [],
      "schedule": [
        {
          "time": "18:00",
          "channel": "CCTV-5",
          "tournament": "WTT欧洲大满贯瑞典站",
          "content": "乒乓球女单1/4决赛"
        }
      ],
      "pending": true
    }
  ],
  "nextEvent": {
    "date": "2026-08-25",
    "weekday": "周二",
    "daysAway": 3,
    "note": "WTT欧洲大满贯瑞典站 1/4决赛"
  }
}
```

---

## 五、video.json 完整规范

### 5.1 顶层结构

`video.json` 顶层**只有**一个 `days` 数组，没有 `title` / `subtitle` / `updatedAt` / `note` / `nextEvent`。

### 5.2 day 对象（`days[]` 每一项）

| 字段 | 类型 | 说明 |
|------|------|------|
| `date` | 字符串 | `YYYY-MM-DD`，与 data.json 同键，网页按日期合并 |
| `matches` | 数组 | 当天视频平台直播的乒乓球场次，见 5.3；当天无视频直播则不写该 `date` |

### 5.3 match 对象（`video.json` 的 `days[].matches[]` 每一项）

> 注意：视频场次用 `platform` 字段（平台名），**不要**用 `channel`。`channel` 是 data.json 央视场次专用的。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `time` | 字符串 | 是 | 开赛时间 `HH:MM` |
| `platform` | 字符串 | 是 | 平台名，如 `咪咕视频` / `央视频` |
| `tournament` | 字符串 | 否 | 赛事名，如 `WTT欧洲大满贯瑞典站` |
| `stage` | 字符串 | 否 | 轮次，如 `女单1/4决赛` |
| `nationHome` | 字符串 | 否 | 主方国籍，如 `中国` / `日本` / `韩国` |
| `playerHome` | 字符串 | 是 | 主方选手名 |
| `playerHomeInfo` | 字符串 | 否 | 主方补充信息，如 `世界第1` |
| `nationAway` | 字符串 | 否 | 客方国籍 |
| `playerAway` | 字符串 | 是 | 客方选手名 |
| `playerAwayInfo` | 字符串 | 否 | 客方补充信息 |
| `note` | 字符串 | 否 | 可选说明 |

### 5.4 video.json 完整示例

```json
{
  "days": [
    {
      "date": "2026-08-22",
      "matches": [
        {
          "time": "19:30",
          "platform": "咪咕视频",
          "tournament": "WTT欧洲大满贯瑞典站",
          "stage": "女单1/4决赛",
          "nationHome": "中国",
          "playerHome": "孙颖莎",
          "playerHomeInfo": "世界第1",
          "nationAway": "日本",
          "playerAway": "张本美和",
          "playerAwayInfo": "世界第2",
          "note": "可选说明"
        }
      ]
    }
  ]
}
```

---

## 六、采集与分流步骤（全量，主任务）

> 速度要求：见第十章「执行速度优化」。核心是**并行扇出采集、多数返回即继续、绝不串行等待**。

1. **并行采集**：一次性派发 3~4 个采集 subagent（按 2.1 来源组分工），后台并行，每个设 5 分钟超时；等齐所有 subagent 或多数已返回即继续。若运行环境无法派发 subagent，则在单回合内对全部来源一次性发出所有 WebFetch（并行），效果等同扇出。
2. **抓取央视场次**：按 2.1 来源清单抓取，提取 CCTV-5 / CCTV-5+ / CCTV-16 转播的乒乓球场次（时间 / 频道 / 对阵 / risk 线索），按 2.2 规则交叉认证。
3. **抓取视频平台场次**：抓取咪咕体育、央视频等来源，提取这些平台直播的乒乓球场次（时间 / 平台 / 对阵）。
4. **判定分流**：
   - `channel` 为 `CCTV-5` / `CCTV-5+` / `CCTV-16` / `央视` 的场次 → 写入 `data.json` 的 `matches`。
   - `platform` 为 `咪咕` / `央视频` / `腾讯体育` / `优酷` / `爱奇艺` / `客户端` / `app` / `网页` / `网端` 的场次 → 写入 `video.json` 的 `matches`。
   - 同一场比赛若既有央视转播又有视频平台直播，两个文件都写（各自按 4.3 / 5.3 格式）。
   - 只放**有直播**的场次；纯录像 / 录播不进任何一个文件。
5. **空结果兜底**：若今日 08:00~次日 08:00 无国乒直播，输出下一场国乒直播的日期 + 时间（同样按 ≥4 源认证），写入 `nextEvent`；并附最近一场国乒比赛结果（写入昨日的 `result`）。
6. **生成 `data.json`**：按第四节完整结构生成（四日期：昨日带 `result` + 今日 + 明日 + 后日；`schedule` 只放乒乓球窗口；`risk` 按 4.4 写法）。
7. **生成 `video.json`**：按第五节完整结构生成（仅 `days[]`；只含有视频直播的日期）。
8. **【铁律·写完即推送】** 写完 `data.json` 并通过 JSON 硬校验后，**立即推送**（命令见第七章 7.1 / 7.2）：`git add data.json [video.json] && git commit && git push origin main`。**此步必须早于早报存档、自动化记忆、单链对齐**——只要它跑完，网页 `updatedAt` 即刷新；之后步骤即使被打断，远端时间也已是最新，杜绝「磁盘已改、远端没推、网页时间没变」的失联。
9. **早报存档**：将本次早报完整内容（含表格与来源清单）以 Markdown 保存到 `每日乒乓球赛事早报/YYYY-MM-DD.md`（文件名用当天日期）。
10. **对齐单链**：执行第九章「9.3 主任务角色」维护刷新 once。

---

## 七、推送命令

> **【铁律·写完即推送】** 写完 `data.json` 并通过 JSON 硬校验后，**立刻**执行本章推送命令，不要等早报存档 / 记忆 / 单链对齐全部跑完再统一推送。任务时序是「写完 data.json → 立即 git add/commit/push → 再做后续」，而**不是**「全部做完 → 最后一起推送」。运行在中后段被打断时，若推送排在最后就会出现「本地磁盘 updatedAt 已改、但远端没推、网页时间没变」的失联——本铁律即为此设。

### 7.1 主任务推送（每日早报，含双文件）

```bash
git add data.json video.json && git commit -m "每日赛程更新 YYYY-MM-DD" && git push origin main
```

### 7.2 轻量刷新推送（只推 data.json，不碰 video.json）

```bash
git add data.json && git commit -m "赛程刷新_YYYY-MM-DD_HH:MM" && git push origin main
```

通用要求：
- 不要使用 `--force` 或强制推送。
- 只推送 GitHub origin；Gitee 备份仓库自 2026-08-09 起已弃用，不再推送。
- **推送前必须先用 `python -c "import json;json.load(open('data.json',encoding='utf-8'))"` 校验 JSON 合法**；非法 JSON 不要 commit（会导致网页解析失败、时间不变）。

---

## 八、注意事项（事实陈述）

- `video.json` 文件名固定为 `video.json`，与 `data.json` 同级目录。
- `video.json` 顶层只有 `days` 数组，不要写 `title` / `subtitle` / `updatedAt` / `nextEvent`。
- `video.json` 的场次用 `platform` 字段；`data.json` 的场次用 `channel` 字段。两者不要混用。
- 某天没有视频平台直播，就不写该 `date` 到 `video.json`（不要写空 `matches` 的日期）。
- 若 `video.json` 缺失或格式错误，网页仍正常显示央视内容，只是不显示视频块，不会报错。
- 网页按 `date` 把 `video.json` 的场次合并进对应日期；视频场次会自动跟随「全部 / 国乒 / 日韩」筛选（依据 `nationHome` / `nationAway`）。

---

## 九、刷新任务编排（单链滚动 + 30 分钟硬隔离）

### 9.1 总体原则

- 维护「**全局唯一**」的下一个刷新 once 任务：同一时刻只允许有 1 个刷新 once 在排期里。
- 硬性保证：任意连续 30 分钟窗口内刷新不超过 1 次。
- 完整保留「赛前 1h 刷新 + 赛中盯比分」两项能力。
- 严禁按旧逻辑逐场建「赛前刷新 / 赛中刷新」once，只能维护唯一单链 once。

### 9.2 刷新时刻计算（赛前 + 赛中合并聚类）

读取最新 `data.json`，生成候选刷新时刻集合 C：

- **赛前候选**：对 `date` ≥ 今日、含具体 `time`、且 `result` 为空的每场，开赛 `K = date + time`（ISO8601 +08:00），`T = K - 1h`。仅当 `T` 落在 `(当前北京时间, 当前 + 30h]` 内才加入 C（`T ≤ 当前` 说明赛前窗已过，交赛中接管；`T > 当前 + 30h` 留给后续轮次）。
- **赛中候选**：对 `date` 为今日或明日的比赛，找「已开赛（当前 > K）且 `result` 为空且 `当前 ≤ K + 3h`（未超 3h，超了判延期停手）」的场次，整体只加 **1 个**「当前 + 30min」轮询点（代表本轮盯比分，不按场多次加）。

对 C 升序排序后做「**30 分钟聚类合并**」：从左到右，若候选与已选簇的末端时刻相差 ≤ 30 分钟，并入同一簇（簇刷新时刻取簇内最晚时刻，保证数据在簇内最末一场前够新鲜）；相差 > 30 分钟则另起新簇。

取「**簇刷新时刻 ≥ 当前 + 30min**」的最早一簇，其时刻记为 `T_next`（本次要排的下一次刷新）。若无任何簇满足 → 本次不建刷新（链暂歇，等主任务 8h 后重新对齐）。

### 9.3 主任务角色（对齐单链）

主任务在完成第六章全量刷新后，本步骤只负责「对齐单链」，不再逐场建 once：

1. `automation_update(mode="list")` 列出所有 name 含「刷新」的 once 任务。
2. 若已有刷新 once 且其 `scheduledAt` 落在 `[当前 - 30min, 当前 + 30min]` 区间内 → 链已在覆盖本窗口，主任务**不再额外刷新数据**，也**不新建** once（若现有多个刷新 once，则删到只留最早一个）。
3. 否则（窗口内无邻近刷新）→ 主任务本身刚完成一次全量刷新，按 9.2 算出 `T_next`，删除现有所有「刷新」once，只新建 1 个单链 once，触发 = `T_next`。
4. 主任务始终产出早报散文与存档（第六章步骤 8），不受是否刷新数据影响。

### 9.4 轻量刷新角色（单链 once 一次性任务）

触发后执行以下流程（**跳过早报散文与表格存档**）：

1. **读取 `data.json`** 了解结构：四日期（昨日带 `result` 战报 + 今日 + 明日 + 后日）；`days[].matches[]` 字段；`subtitle` 固定「央视直播 · 乒乓球赛程 · 每日更新(可筛选 全部/国乒/日韩)」。
2. **并行采集**：一次性派发 3~4 个采集 subagent（按 2.1 来源组分工），后台并行，每个 5 分钟超时；等齐所有或多数已返回即继续。若环境无法派发 subagent，则在单回合内对来源池一次性发出全部 WebFetch（并行）再按相同规则合并。同一内容被多家转载不算独立源。
3. **交叉认证**：汇总各 subagent 回传，按日期归并；同场被多组报到 → 合并证据不重复计数；时间取多数源众数，差异在备注；**昨日战报块从现有 data.json 原样保留**，只刷新今 / 明 / 后三日。
4. **更新 `data.json`**：只改 `matches` 的 `result`（已结束写精简比分如 `向鹏 3-1 西多伦科(11-8/9-11/11-9/11-8)`）、`risk`（未结束按 4.4 写法）、`time` / `channel` 与必要 `schedule`；保留四日期结构与昨日战报；`subtitle` 维持原值；`updatedAt` = 当前日期时间；`schedule` 只放乒乓球窗口，剔除录像 / 录播与非乒乓球栏目。
5. **【铁律·写完即推送】** 更新 `data.json` 并 JSON 硬校验通过后，**立即**执行第七章 7.2 推送命令（只推 `data.json`，不碰 `video.json`）。此步必须早于任何收尾动作，保证网页 `updatedAt` 立刻刷新。
6. **自我延续（单链滚动，30 分钟硬隔离）**：
   - 重跑 9.2「刷新时刻计算」算出 `T_next`。
   - `automation_update(mode="list")` 列出所有 name 含「刷新」的 once 任务，删除现有所有「刷新」once（腾出，保证唯一）。
   - 若算出 `T_next`：新建 1 个 once（命名与字段见 9.5），其 prompt 同样声明「你是轻量刷新」，读取本文件并按 9.4 执行（含本步骤 6 单链逻辑，且带 `modelId:"hy3"`）。
   - 若算不出 `T_next`：不建，链暂停，等主任务 8h 后重新对齐（不要遗留任何刷新 once）。

### 9.5 命名与字段约定（刷新 once）

- 单链 once 统一命名 `刷新链-[YYYYMMDD]-[HHMM]`；建前先 `mode="list"` 查同名 / 邻近，避免重复。
- 所有刷新 once 字段：
  - `cwds` = `Users/pengzishang/WorkBuddy/乒乓球`（**无前导斜杠**）
  - `scheduleType` = `"once"`
  - `scheduledAt` = ISO8601 时间（+08:00）
  - **`modelId` = `"hy3"`**（必须显式写，self-spawn 子任务不继承父 modelId）

---

## 十、执行速度优化（编排原则）

1. **采集全并行，绝不串行等待**：所有 subagent 一次性派发、后台并行，每个 5 分钟超时；「等齐所有 或 多数已返回」即继续收口，不傻等最慢的那个。降级时单回合并行发出全部 WebFetch。
2. **角色分离，增量优先**：
   - 主任务（每 8h）做**全量**：采集 + 早报 + `data.json` + `video.json` + 推送 + 对齐单链。
   - 轻量刷新（once）做**增量**：只更新 `result` / `risk` / `time` / `channel` 与必要 `schedule`，跳过早报散文、不重抓昨日、**不碰 `video.json`**。
3. **避免重复刷新**：主任务发现窗口内已有刷新 once 覆盖时，跳过再次刷新数据，只删多余 once（留最早一个）。
4. **单链唯一 + 30 分钟聚类**：同一时刻只有 1 个刷新 once，任意 30 分钟窗口最多刷 1 次；赛中盯比分整体合并成 1 个轮询点，不按场多次建。
5. **只推有变化的文件**：轻量刷新只 `git add data.json`，不重推未变的 `video.json`。

---

## 十一、质量要求

- 保持信息准确，不确定的信息不要编造；来源冲突、证据不足、译名歧义（如希德仁科 = 西多伦科）、插播风险必须如实披露。
- 用中文，以简洁、亲和的方式播报。
- 如央视官网无法直接抓取，如实说明并以其他独立来源交叉验证为准。
